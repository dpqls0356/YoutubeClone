extends ../base
include ../mixins/video
block content
    div.profile-box
        div.user-info 
            img(src="/"+user.avatarUrl)
            span=user.name
        div.view_small_video
            each video in user.videos
                +video(video)
